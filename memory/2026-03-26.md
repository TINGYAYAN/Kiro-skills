Web 控制台初版已落地到 `web_console/`。

- 新增本地服务：`web_console/app.py`
- 页面功能：项目总览、单条生成、批量生成、历史记录、配置管理
- 本地验证：
  - `python3 -m py_compile web_console/app.py` 通过
  - `python3 web_console/app.py --port 8766` 可启动
  - `GET /api/dashboard` 正常返回项目 / session / 证书 / 飞书表信息
  - `POST /api/run/batch` + `dry_run` 正常创建任务并写入 `web_console/runtime/*.log`
- 修复：
  - `web_console/app.py` 作为子目录脚本启动时找不到 `utils`，已补 `sys.path`
  - `batch_runner.py` 新增 `--project`，支持按飞书表 `项目` 列精确过滤
- `BOOTSTRAP.md` 已删除
