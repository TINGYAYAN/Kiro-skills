2026-03-27

- 修复线上 Web 控制台单条/批量任务子进程解释器问题：`web_console/app.py` 启动 `pipeline.py`、`batch_runner.py`、`run_uploaded_batch.py` 时不再使用裸 `python3`，统一改为 `sys.executable`。
- 根因：服务器 `.venv` 已安装 `openai`，但子任务此前走系统 Python，导致线上任务报 `ModuleNotFoundError: No module named 'openai'`。
- 以后凡是 Web 控制台里发起子进程，都优先复用当前进程解释器，避免本地与服务器环境不一致。
