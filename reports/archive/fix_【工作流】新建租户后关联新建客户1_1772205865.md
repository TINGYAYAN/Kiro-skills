# APL 修复报告：【工作流】新建租户后关联新建客户1

生成时间：2026-02-27 23:24:25

共修复 6 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-45671941
StopWatch 'Proc_X8PMz__c': running time = 107 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

expecting ':', found ')' @ line 51, column 49.
```

**代码变更（unified diff）：**
```diff
--- 第0次
+++ 第1次
@@ -1,3 +1,5 @@
+错误在第51行，`FQLAttribute.builder()` 链式调用中使用了 `.limit(1)` 方法，该方法不存在于 `FQLAttribute`。移除 `.limit(1)` 即可修复。
+
 /**
  * @author 纷享-颜婷雅
  * @codeName 【工作流】新建租户后关联新建客户1
@@ -23,7 +25,6 @@
     FQLAttribute.builder()
         .columns(["_id"])
         .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code": QueryOperator.EQ(orgCode)]))
-        .limit(1)
         .build(),
     SelectAttribute.builder().build()
 )

```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-27329522
StopWatch 'Proc_X8PMz__c': running time = 267 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 38.
```

**代码变更（unified diff）：**
```diff
--- 第1次
+++ 第2次
@@ -1,5 +1,3 @@
-错误在第51行，`FQLAttribute.builder()` 链式调用中使用了 `.limit(1)` 方法，该方法不存在于 `FQLAttribute`。移除 `.limit(1)` 即可修复。
-
 /**
  * @author 纷享-颜婷雅
  * @codeName 【工作流】新建租户后关联新建客户1

```

## 第 3 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-69475258
StopWatch 'Proc_X8PMz__c': running time = 340 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

expecting ':', found ')' @ line 50, column 49.
```

**代码变更（unified diff）：**
```diff
--- 第2次
+++ 第3次
@@ -1,3 +1,9 @@
+错误在第50行，`UpdateAttribute.builder().triggerWorkflow(false).build()` 中 `false` 被解析有问题，通常是 Groovy 静态编译下布尔字面量在某些链式调用中需要显式类型。但更常见的原因是 `updateResult?["_id"]` 中的 `?` 安全导航在静态类型检查下不支持 Map 下标，需改为 `updateResult?.get("_id")`。
+
+不过报错 `expecting ':', found ')'` 指向第50列，最可能是 `Fx.object.update` 调用中某个参数的语法问题。检查原代码行数，第50行是 `UpdateAttribute.builder().triggerWorkflow(false).build()`——实际上错误更可能来自 `updateResult?["_id"]` 这种写法在静态检查下的解析歧义。
+
+修复点：将 `updateResult?["_id"]` 改为 `updateResult?.get("_id")`。
+
 /**
  * @author 纷享-颜婷雅
  * @codeName 【工作流】新建租户后关联新建客户1
@@ -33,7 +39,7 @@
 
 String customerId = ""
 if (findResult && findResult.dataList && findResult.dataList.size() > 0) {
-    customerId = ((findResult.dataList[0] as Map)["_id"] as String) ?: ""
+    customerId = ((findResult.dataList[0] as Map).get("_id") as String) ?: ""
 }
 
 if (!customerId) {
@@ -47,7 +53,7 @@
         log.error("创建客户失败：" + createMsg)
         return
     }
-    customerId = (createResult?["_id"] as String) ?: ""
+    customerId = (createResult?.get("_id") as String) ?: ""
     if (!customerId) {
         log.error("创建客户后未获取到客户ID")
         return
@@ -63,5 +69,5 @@
 if (updateErr) {
     log.error("关联客户失败：" + updateMsg)
 } else {
-    log.info("租户已关联客户：" + updateResult?["_id"])
+    log.info("租户已关联客户：" + updateResult?.get("_id"))
 }
```

## 第 4 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-18259803
StopWatch 'Proc_X8PMz__c': running time = 322 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 38.
```

**代码变更（unified diff）：**
```diff
--- 第3次
+++ 第4次
@@ -1,20 +1,7 @@
-错误在第50行，`UpdateAttribute.builder().triggerWorkflow(false).build()` 中 `false` 被解析有问题，通常是 Groovy 静态编译下布尔字面量在某些链式调用中需要显式类型。但更常见的原因是 `updateResult?["_id"]` 中的 `?` 安全导航在静态类型检查下不支持 Map 下标，需改为 `updateResult?.get("_id")`。
+错误 `unexpected char: '`' @ line 1, column 38` 说明代码中混入了反引号字符（backtick `` ` ``），通常是从富文本编辑器粘贴时引入的。检查 `customerId = ""` 或字符串赋值处是否有隐藏的反引号。
 
-不过报错 `expecting ':', found ')'` 指向第50列，最可能是 `Fx.object.update` 调用中某个参数的语法问题。检查原代码行数，第50行是 `UpdateAttribute.builder().triggerWorkflow(false).build()`——实际上错误更可能来自 `updateResult?["_id"]` 这种写法在静态检查下的解析歧义。
+修复后完整代码：
 
-修复点：将 `updateResult?["_id"]` 改为 `updateResult?.get("_id")`。
-
-/**
- * @author 纷享-颜婷雅
- * @codeName 【工作流】新建租户后关联新建客户1
- * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。
- * 如果不是，则新建一个客户，并关联租户到此客户。
- * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。
- * @createTime 2026-02-27
- * @bindingObjectLabel 租户
- * @bindingObjectApiName tenant__c
- * @函数需求编号
- */
 String tenantId = (context.data["_id"] as String) ?: ""
 String orgCode = (context.data["organization_code__c"] as String) ?: ""
 String tenantName = (context.data["company_name__c"] as String) ?: ""

```

## 第 5 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-81419353
StopWatch 'Proc_X8PMz__c': running time = 302 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 33.
```

**代码变更（unified diff）：**
```diff
--- 第4次
+++ 第5次
@@ -1,6 +1,8 @@
-错误 `unexpected char: '`' @ line 1, column 38` 说明代码中混入了反引号字符（backtick `` ` ``），通常是从富文本编辑器粘贴时引入的。检查 `customerId = ""` 或字符串赋值处是否有隐藏的反引号。
+错误提示 `unexpected char: '`' @ line 1, column 33` 说明第1行第33列有反引号。检查代码第1行：
 
-修复后完整代码：
+`String tenantId = (context.data["_id"] as String) ?: ""`
+
+`context` 前面或 `["_id"]` 里混入了反引号。修复后完整代码：
 
 String tenantId = (context.data["_id"] as String) ?: ""
 String orgCode = (context.data["organization_code__c"] as String) ?: ""

```

## 第 6 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-99533685
StopWatch 'Proc_X8PMz__c': running time = 246 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 35.
```

**代码变更（unified diff）：**
```diff
--- 第5次
+++ 第6次
@@ -1,9 +1,3 @@
-错误提示 `unexpected char: '`' @ line 1, column 33` 说明第1行第33列有反引号。检查代码第1行：
-
-`String tenantId = (context.data["_id"] as String) ?: ""`
-
-`context` 前面或 `["_id"]` 里混入了反引号。修复后完整代码：
-
 String tenantId = (context.data["_id"] as String) ?: ""
 String orgCode = (context.data["organization_code__c"] as String) ?: ""
 String tenantName = (context.data["company_name__c"] as String) ?: ""
@@ -42,11 +36,12 @@
         log.error("创建客户失败：" + createMsg)
         return
     }
-    customerId = (createResult?.get("_id") as String) ?: ""
-    if (!customerId) {
+    String newId = (createResult?.get("_id") as String) ?: ""
+    if (!newId) {
         log.error("创建客户后未获取到客户ID")
         return
     }
+    customerId = newId
 }
 
 def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update(

```

