# APL 修复报告：【流程】“介绍人”这个字段上

生成时间：2026-03-18 18:29:52

共修复 3 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-33043151
StopWatch 'Proc_HDBQc__c': running time = 281 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unable to resolve class FQLAttribute.Builder 
 @ line 33, column 22.
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -1,9 +1,9 @@
 /**
  * @author 纷享实施人员
- * @codeName 【流程】“介绍人”这个字段上
- * @description 在“介绍人”这个字段上增加一个范围规则函数
+ * @codeName 【流程】"介绍人"这个字段上
+ * @description 在"介绍人"这个字段上增加一个范围规则函数
  * 函数内容如下
- * 当当前客户的业务类型等于“ 门店”介绍人的范围规则仅能显示业务类型为“ 门店”的客户
- *  * 客户的业务类型等于“ 用户”介绍人的范围规则仅能显示业务类型为“ 用户”的客户
+ * 当当前客户的业务类型等于" 门店"介绍人的范围规则仅能显示业务类型为" 门店"的客户
+ *  * 客户的业务类型等于" 用户"介绍人的范围规则仅能显示业务类型为" 用户"的客户
  *   获取不到业务类型的情况下，显示全部类型的客户
  * @createTime 2026-03-18
@@ -12,5 +12,4 @@
  * @函数需求编号
  */
-// ----- 1. 取当前客户业务类型 -----
 String businessType = context.data.c_classification__c as String
 if (!businessType) businessType = ""
@@ -18,5 +17,4 @@
 log.info("[业务] 入参: businessType=" + businessType)
 
-// ----- 2. 根据业务类型构建查询条件 -----
 Map queryMap = [:]
 if (businessType == "门店") {
@@ -30,13 +28,11 @@
 }
 
-// ----- 3. 查询符合条件的客户 -----
-FQLAttribute.Builder fqlBuilder = FQLAttribute.builder().columns(["_id", "name"])
-if (queryMap.size() > 0) {
-    fqlBuilder.queryTemplate(QueryTemplate.AND(queryMap))
-}
+FQLAttribute fqlAttr = queryMap.size() > 0 
+    ? FQLAttribute.builder().columns(["_id", "name"]).queryTemplate(QueryTemplate.AND(queryMap)).build()
+    : FQLAttribute.builder().columns(["_id", "name"]).build()
 
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "AccountObj",
-    fqlBuilder.build(),
+    fqlAttr,
     SelectAttribute.builder().build()
 )

```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-8043354
18:29:04:125 [业务] 入参: businessType=
18:29:04:129 [业务] 业务类型为空或其他，介绍人范围显示全部客户
18:29:04:131 [业务] 查询客户失败: parameter cannot be empty
StopWatch 'Proc_HDBQc__c': running time = 270 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
```

**代码变更（unified diff）：**
```diff
--- v1
+++ v2
@@ -17,29 +17,37 @@
 log.info("[业务] 入参: businessType=" + businessType)
 
-Map queryMap = [:]
 if (businessType == "门店") {
-    queryMap.put("c_classification__c", QueryOperator.EQ("门店"))
+    Map queryMap = ["c_classification__c": QueryOperator.EQ("门店")]
     log.info("[业务] 业务类型为门店，介绍人范围限制为门店客户")
+    
+    def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
+        "AccountObj",
+        FQLAttribute.builder().columns(["_id", "name"]).queryTemplate(QueryTemplate.AND(queryMap)).build(),
+        SelectAttribute.builder().build()
+    )
+    
+    if (findErr) {
+        log.error("[业务] 查询客户失败: " + findMsg)
+        return
+    }
+    
+    log.info("[业务] 完成: 查询到 " + (findResult?.size ?: 0) + " 条客户记录")
 } else if (businessType == "用户") {
-    queryMap.put("c_classification__c", QueryOperator.EQ("用户"))
+    Map queryMap = ["c_classification__c": QueryOperator.EQ("用户")]
     log.info("[业务] 业务类型为用户，介绍人范围限制为用户客户")
+    
+    def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
+        "AccountObj",
+        FQLAttribute.builder().columns(["_id", "name"]).queryTemplate(QueryTemplate.AND(queryMap)).build(),
+        SelectAttribute.builder().build()
+    )
+    
+    if (findErr) {
+        log.error("[业务] 查询客户失败: " + findMsg)
+        return
+    }
+    
+    log.info("[业务] 完成: 查询到 " + (findResult?.size ?: 0) + " 条客户记录")
 } else {
     log.info("[业务] 业务类型为空或其他，介绍人范围显示全部客户")
-}
-
-FQLAttribute fqlAttr = queryMap.size() > 0 
-    ? FQLAttribute.builder().columns(["_id", "name"]).queryTemplate(QueryTemplate.AND(queryMap)).build()
-    : FQLAttribute.builder().columns(["_id", "name"]).build()
-
-def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
-    "AccountObj",
-    fqlAttr,
-    SelectAttribute.builder().build()
-)
-
-if (findErr) {
-    log.error("[业务] 查询客户失败: " + findMsg)
-    return
-}
-
-log.info("[业务] 完成: 查询到 " + (findResult?.size ?: 0) + " 条客户记录")+}
```

## 第 3 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-40785561
18:29:14:216 [业务] 入参: businessType=
18:29:14:219 [业务] 业务类型为空或其他，介绍人范围显示全部客户
18:29:14:220 [业务] 查询客户失败: parameter cannot be empty
StopWatch 'Proc_HDBQc__c': running time = 105 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -13,27 +13,10 @@
  */
 String businessType = context.data.c_classification__c as String
-if (!businessType) businessType = ""
 
-log.info("[业务] 入参: businessType=" + businessType)
+log.info("[业务] 入参: businessType=" + (businessType ?: ""))
 
-if (businessType == "门店") {
-    Map queryMap = ["c_classification__c": QueryOperator.EQ("门店")]
-    log.info("[业务] 业务类型为门店，介绍人范围限制为门店客户")
-    
-    def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
-        "AccountObj",
-        FQLAttribute.builder().columns(["_id", "name"]).queryTemplate(QueryTemplate.AND(queryMap)).build(),
-        SelectAttribute.builder().build()
-    )
-    
-    if (findErr) {
-        log.error("[业务] 查询客户失败: " + findMsg)
-        return
-    }
-    
-    log.info("[业务] 完成: 查询到 " + (findResult?.size ?: 0) + " 条客户记录")
-} else if (businessType == "用户") {
-    Map queryMap = ["c_classification__c": QueryOperator.EQ("用户")]
-    log.info("[业务] 业务类型为用户，介绍人范围限制为用户客户")
+if (businessType == "门店" || businessType == "用户") {
+    Map queryMap = ["c_classification__c": QueryOperator.EQ(businessType)]
+    log.info("[业务] 业务类型为" + businessType + "，介绍人范围限制为" + businessType + "客户")
     
     def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
@@ -51,3 +34,16 @@
 } else {
     log.info("[业务] 业务类型为空或其他，介绍人范围显示全部客户")
+    
+    def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
+        "AccountObj",
+        FQLAttribute.builder().columns(["_id", "name"]).build(),
+        SelectAttribute.builder().build()
+    )
+    
+    if (findErr) {
+        log.error("[业务] 查询客户失败: " + findMsg)
+        return
+    }
+    
+    log.info("[业务] 完成: 查询到 " + (findResult?.size ?: 0) + " 条客户记录")
 }
```

