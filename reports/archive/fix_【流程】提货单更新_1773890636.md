# APL 修复报告：【流程】提货单更新

生成时间：2026-03-19 11:23:56

共修复 3 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-16493204
StopWatch 'Proc_PDic2__c': running time = 215 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

SecurityException: ForStatements are not allowed
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -8,5 +8,4 @@
  * @函数需求编号
  */
-// ----- 1. 取当前提货单的门店名称 -----
 String deliveryId = context.data._id as String
 String storeId = context.data.account_id__c as String
@@ -23,5 +22,4 @@
 }
 
-// ----- 2. 查询该门店下生命状态为正常的提货单 -----
 log.info("[业务] 查询门店下生命状态为正常的提货单: storeId=" + storeId)
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
@@ -49,13 +47,12 @@
 }
 
-// ----- 3. 统计3个月内审核通过的提货总额 -----
 long threeMonthsAgo = System.currentTimeMillis() - (90L * 24 * 60 * 60 * 1000)
 BigDecimal totalAmount = BigDecimal.ZERO
 
 List dataList = findResult.dataList as List
-for (int i = 0; i < dataList.size(); i++) {
-    Map item = dataList[i] as Map
-    Object passingDateObj = item.get("passing_date__c")
-    Object amountObj = item.get("total_amount__c")
+dataList.each { item ->
+    Map itemMap = item as Map
+    Object passingDateObj = itemMap.get("passing_date__c")
+    Object amountObj = itemMap.get("total_amount__c")
     
     if (passingDateObj && amountObj) {
@@ -81,5 +78,4 @@
 log.info("[业务] 统计完成: 3个月内提货总额=" + totalAmount.toString())
 
-// ----- 4. 更新客户的近一个月回款字段 -----
 log.info("[业务] 更新客户近一个月回款: storeId=" + storeId + ", amount=" + totalAmount.toString())
 def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update(

```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-69207201
StopWatch 'Proc_PDic2__c': running time = 238 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.time.Date#getTime(). Please check if the declared type is correct and if the method exists.
 @ line 63, column 27.
```

**代码变更（unified diff）：**
```diff
--- v1
+++ v2
@@ -2,5 +2,5 @@
  * @author 纷享实施人员
  * @codeName 【流程】提货单更新
- * @description 生成一个流程函数。查询提货单上的门店名称，查询当前门店名称下的生命状态为正常的提货单，统计提货单上审核通过日期为3个月内的提货总额（元），汇总后更新至客户的近一个月回款字段内。
+ * @description 生成一个流程函数。查询提货单上的门店名称,查询当前门店名称下的生命状态为正常的提货单,统计提货单上审核通过日期为3个月内的提货总额(元),汇总后更新至客户的近一个月回款字段内。
  * @createTime 2026-03-19
  * @bindingObjectLabel 提货单
@@ -60,6 +60,6 @@
         if (passingDateObj instanceof Long) {
             passingTime = passingDateObj as Long
-        } else if (passingDateObj instanceof Date) {
-            passingTime = (passingDateObj as Date).getTime()
+        } else if (passingDateObj instanceof java.util.Date) {
+            passingTime = (passingDateObj as java.util.Date).getTime()
         }
         

```

## 第 3 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-80429527
11:23:34:035 [业务] 入参: deliveryId=69b9080fadebd50007f4a5a3, storeId=69b8e36297d9cb0007ae1d04
11:23:34:038 [业务] 查询门店下生命状态为正常的提货单: storeId=69b8e36297d9cb0007ae1d04
11:23:34:051 [业务] 查询提货单失败: The number of keys in the map cannot exceed 1
StopWatch 'Proc_PDic2__c': running time = 152 ms
---------------------------------------------
ms % Task name
--------------
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -27,8 +27,8 @@
     FQLAttribute.builder()
         .columns(["_id", "passing_date__c", "total_amount__c"])
-        .queryTemplate(QueryTemplate.AND([
-            "account_id__c": QueryOperator.EQ(storeId),
-            "life_status": QueryOperator.EQ(1)
-        ]))
+        .queryTemplate(QueryTemplate.AND(
+            QueryTemplate.EQ("account_id__c", storeId),
+            QueryTemplate.EQ("life_status", 1)
+        ))
         .build(),
     SelectAttribute.builder().build()
@@ -39,6 +39,4 @@
     return
 }
-
-log.info("[业务][诊断] 查询结果: " + findResult?.toString())
 
 if (!findResult || !findResult.dataList || findResult.dataList.size() == 0) {

```

