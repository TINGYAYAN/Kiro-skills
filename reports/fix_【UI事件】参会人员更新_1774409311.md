# APL 修复报告：【UI事件】参会人员更新

生成时间：2026-03-25 11:28:31

共修复 1 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-83869274
StopWatch 'UIEvt_DbzcJ__c': running time = 250 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.ui.UIEvent#save(). Please check if the declared type is correct and if the method exists.
 @ line 18, column 12.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.ui.UIEvent#save(). Please check if the declared type is correct and if the method exists.
 @ line 22, 
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -16,9 +16,9 @@
 if (!recordId) {
     log.error("[业务] 终止: 当前记录ID为空")
-    return UIEvent.build(context) {}.save()
+    return UIEvent.build(context) {}
 }
 if (!customerNameId) {
     log.error("[业务] 终止: 顾客姓名为空")
-    return UIEvent.build(context) {}.save()
+    return UIEvent.build(context) {}
 }
 
@@ -36,10 +36,10 @@
 if (findErr) {
     log.error("[业务] 查询客户失败: " + findMsg)
-    return UIEvent.build(context) {}.save()
+    return UIEvent.build(context) {}
 }
 
 if (!findResult || !findResult.dataList || findResult.dataList.size() == 0) {
     log.error("[业务] 终止: 未查到对应客户记录")
-    return UIEvent.build(context) {}.save()
+    return UIEvent.build(context) {}
 }
 
@@ -51,5 +51,5 @@
 if (!storeId) {
     log.error("[业务] 终止: 归属门店为空")
-    return UIEvent.build(context) {}.save()
+    return UIEvent.build(context) {}
 }
 
@@ -65,8 +65,8 @@
 if (updateErr) {
     log.error("[业务] 更新门店名称失败: " + updateMsg)
-    return UIEvent.build(context) {}.save()
+    return UIEvent.build(context) {}
 }
 
 log.info("[业务] 完成: 门店名称已更新, storeId=" + storeId + ", updateResult=" + updateResult?.toString())
 
-return UIEvent.build(context) {}.save()+return UIEvent.build(context) {}
```

