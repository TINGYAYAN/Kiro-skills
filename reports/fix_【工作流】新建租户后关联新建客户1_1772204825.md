# APL 修复报告：【工作流】新建租户后关联新建客户1

生成时间：2026-02-27 23:07:05

共修复 1 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-95523153
StopWatch 'Proc_Md8ZD__c': running time = 276 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.api.ObjectDataAPI#create(java.lang.String, java.util.LinkedHashMap , com.fxiaoke.functions.tools.CreateAttribute). Please check if the declared type is correct and if the method exists.
 @ line 41, column 67.
```

**代码变更（unified diff）：**
```diff
--- 第0次
+++ 第1次
@@ -1,14 +1,3 @@
-/**
- * @author 纷享-颜婷雅
- * @codeName 【工作流】新建租户后关联新建客户1
- * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。
- * 如果不是，则新建一个客户，并关联租户到此客户。
- * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。
- * @createTime 2026-02-27
- * @bindingObjectLabel 租户
- * @bindingObjectApiName tenant__c
- * @函数需求编号
- */
 String tenantId = context.data["_id"] as String
 String orgCode = (context.data["organization_code__c"] ?: "") as String
 String tenantName = (context.data["company_name__c"] ?: "") as String
@@ -18,14 +7,12 @@
     return
 }
 
-// 按组织机构代码查找已有客户
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "AccountObj",
     FQLAttribute.builder()
         .columns(["_id"])
         .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)]))
-        .build(),
-    SelectAttribute.builder().build()
+        .build()
 )
 
 if (findErr) {
@@ -38,13 +25,14 @@
 if (findResult && findResult.size > 0 && findResult.dataList) {
     customerId = (findResult.dataList[0] as Map)["_id"] as String
 } else {
+    Map<String, Object> createData = [
+        "name": (tenantName ?: orgCode),
+        "uniform_social_credit_code__c": orgCode
+    ] as Map<String, Object>
+
     def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create(
         "AccountObj",
-        [
-            "name": (tenantName ?: orgCode),
-            "uniform_social_credit_code__c": orgCode
-        ],
-        CreateAttribute.builder().triggerWorkflow(false).build()
+        createData
     )
     if (createErr) {
         log.error("创建客户失败：" + createMsg)
@@ -58,11 +46,12 @@
     return
 }
 
+Map<String, Object> updateData = ["customer__c": customerId] as Map<String, Object>
+
 def (Boolean updateErr, Map _, String updateMsg) = Fx.object.update(
     "tenant__c",
     tenantId,
-    ["customer__c": customerId],
-    UpdateAttribute.builder().triggerWorkflow(false).build()
+    updateData
 )
 
 if (updateErr) {

```

