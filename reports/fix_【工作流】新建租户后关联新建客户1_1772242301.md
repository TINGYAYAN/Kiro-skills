# APL 修复报告：【工作流】新建租户后关联新建客户1

生成时间：2026-02-28 09:31:41

共修复 4 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-61571507
StopWatch 'Proc_arbSf__c': running time = 120 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

expecting EOF, found 'String' @ line 12, column 49.
```

**代码变更（unified diff）：**
```diff
--- 第0次
+++ 第1次
@@ -1,12 +1 @@
-/**
- * @author 纷享-颜婷雅
- * @codeName 【工作流】新建租户后关联新建客户1
- * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。
- * 如果不是，则新建一个客户，并关联租户到此客户。
- * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。
- * @createTime 2026-02-28
- * @bindingObjectLabel 租户
- * @bindingObjectApiName tenant__c
- * @函数需求编号
- */
-String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } customerId = createResult["_id"] as String } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }+错误原因是多条语句写在同一行，Groovy 解析器遇到 `as String String` 时报错。每条语句必须单独一行。 /** * @author 纷享-颜婷雅 * @codeName 【工作流】新建租户后关联新建客户1 * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 * 如果不是，则新建一个客户，并关联租户到此客户。 * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 * @createTime 2026-02-28 * @bindingObjectLabel 租户 * @bindingObjectApiName tenant__c * @函数需求编号 */ String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }
```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-80071927
StopWatch 'Proc_arbSf__c': running time = 123 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 58.
```

**代码变更（unified diff）：**
```diff
--- 第1次
+++ 第2次
@@ -1 +1 @@
-错误原因是多条语句写在同一行，Groovy 解析器遇到 `as String String` 时报错。每条语句必须单独一行。 /** * @author 纷享-颜婷雅 * @codeName 【工作流】新建租户后关联新建客户1 * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 * 如果不是，则新建一个客户，并关联租户到此客户。 * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 * @createTime 2026-02-28 * @bindingObjectLabel 租户 * @bindingObjectApiName tenant__c * @函数需求编号 */ String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }+/** * @author 纷享-颜婷雅 * @codeName 【工作流】新建租户后关联新建客户1 * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 * 如果不是，则新建一个客户，并关联租户到此客户。 * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 * @createTime 2026-02-28 * @bindingObjectLabel 租户 * @bindingObjectApiName tenant__c * @函数需求编号 */ String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }
```

## 第 3 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-15682659
StopWatch 'Proc_arbSf__c': running time = 268 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

expecting EOF, found 'String' @ line 1, column 354.
```

**代码变更（unified diff）：**
```diff
--- 第2次
+++ 第3次
@@ -1 +1 @@
-/** * @author 纷享-颜婷雅 * @codeName 【工作流】新建租户后关联新建客户1 * @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 * 如果不是，则新建一个客户，并关联租户到此客户。 * 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 * @createTime 2026-02-28 * @bindingObjectLabel 租户 * @bindingObjectApiName tenant__c * @函数需求编号 */ String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }+错误原因是注释块里有换行，平台把整个脚本压成一行后，`*/` 后面紧跟 `String` 导致解析失败。需要把块注释改成单行注释，同时确认所有 API 签名正确。 // @author 纷享-颜婷雅 // @codeName 【工作流】新建租户后关联新建客户1 // @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 // 如果不是，则新建一个客户，并关联租户到此客户。 // 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 // @createTime 2026-02-28 // @bindingObjectLabel 租户 // @bindingObjectApiName tenant__c String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }
```

## 第 4 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.gjldkj2025.1001-22958477
StopWatch 'Proc_arbSf__c': running time = 224 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 56.
```

**代码变更（unified diff）：**
```diff
--- 第3次
+++ 第4次
@@ -1 +1 @@
-错误原因是注释块里有换行，平台把整个脚本压成一行后，`*/` 后面紧跟 `String` 导致解析失败。需要把块注释改成单行注释，同时确认所有 API 签名正确。 // @author 纷享-颜婷雅 // @codeName 【工作流】新建租户后关联新建客户1 // @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 // 如果不是，则新建一个客户，并关联租户到此客户。 // 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 // @createTime 2026-02-28 // @bindingObjectLabel 租户 // @bindingObjectApiName tenant__c String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }+// @author 纷享-颜婷雅 // @codeName 【工作流】新建租户后关联新建客户1 // @description 新建租户时，根据组织机构代码来查找是否有已有的客户，如果是，则将租户关联到此客户。 // 如果不是，则新建一个客户，并关联租户到此客户。 // 新建客户时，将租户上的企业名称和组织机构代码赋值到客户的客户名称和统一信用代码字段。 // @createTime 2026-02-28 // @bindingObjectLabel 租户 // @bindingObjectApiName tenant__c String tenantId = context.data["_id"] as String String orgCode = context.data["organization_code__c"] as String String companyName = context.data["company_name__c"] as String if (!tenantId || !orgCode) { log.error("租户ID或组织机构代码为空") return } def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find( "AccountObj", FQLAttribute.builder() .columns(["_id"]) .queryTemplate(QueryTemplate.AND(["uniform_social_credit_code__c": QueryOperator.EQ(orgCode)])) .build(), SelectAttribute.builder().build() ) if (findErr) { log.error("查询客户失败：" + findMsg) return } String customerId = "" if (findResult != null && findResult.dataList != null && findResult.dataList.size() > 0) { customerId = (findResult.dataList[0] as Map)["_id"] as String } if (!customerId) { def (Boolean createErr, Map createResult, String createMsg) = Fx.object.create( "AccountObj", [ "name": (companyName ?: orgCode), "uniform_social_credit_code__c": orgCode ] as Map<String, Object>, [:], CreateAttribute.builder().triggerWorkflow(false).build() ) if (createErr) { log.error("创建客户失败：" + createMsg) return } String newId = createResult?["_id"] as String customerId = newId } def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update( "tenant__c", tenantId, ["customer__c": customerId] as Map<String, Object>, UpdateAttribute.builder().triggerWorkflow(false).build() ) if (updateErr) { log.error("关联客户失败：" + updateMsg) } else { log.info("租户关联客户成功，customerId=" + updateResult?["_id"]) }
```

