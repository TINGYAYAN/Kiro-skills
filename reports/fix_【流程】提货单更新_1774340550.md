# APL 修复报告：【流程】提货单更新

生成时间：2026-03-24 16:22:30

共修复 6 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-71721754
16:20:27:820 [业务] 入参: deliveryOrderId=69c0b28756646900086dae54, accountId=69ba0e00d4d66f000716061d
16:20:27:828 [业务] 查询条件: accountId=69ba0e00d4d66f000716061d, passing_date__c >= 1766564427828
16:20:27:832 [业务] 查询提货单失败: The number of keys in the map cannot exceed 1
StopWatch 'Proc_Hb4Jz__c': running time = 269 ms
--------------------------------------------
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -1,15 +1,4 @@
-/**
- * @author 纷享实施人员
- * @codeName 【流程】提货单更新
- * @description 生成一个流程函数
- * 查询提货单上的“门店名称”
- * 查询当前门店名称下的生命状态为正常的提货单，
- * 统计提货单上“审核通过日期”为3个月内的提货总额（元），汇总后更新至客户的“近一个月回款”字段内
- * @createTime 2026-03-24
- * @bindingObjectLabel 提货单
- * @bindingObjectApiName DeliveryOrderObj
- * @函数需求编号
- */
-// ----- 1. 取当前提货单数据 -----
+错误原因：`QueryTemplate.AND()` 传入的 Map 超过1个 key 时报错 `The number of keys in the map cannot exceed 1`，需改为将多个条件以列表形式传入。
+
 String deliveryOrderId = context.data["_id"] as String
 String accountId = context.data["account_id__c"] as String
@@ -19,9 +8,7 @@
 if (!accountId) { log.error("[业务] 终止: 门店名称(account_id__c)为空"); return }
 
-// ----- 2. 计算3个月前时间戳 -----
 long threeMonthsAgo = System.currentTimeMillis() - (90L * 24 * 60 * 60 * 1000)
 log.info("[业务] 查询条件: accountId=" + accountId + ", passing_date__c >= " + threeMonthsAgo)
 
-// ----- 3. 查询同门店、生命状态正常、审核通过日期在3个月内的提货单 -----
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "DeliveryOrderObj",
@@ -29,7 +16,7 @@
         .columns(["_id", "total_amount__c"])
         .queryTemplate(QueryTemplate.AND([
-            "account_id__c"  : QueryOperator.EQ(accountId),
-            "life_status"    : QueryOperator.EQ("normal"),
-            "passing_date__c": QueryOperator.GTE(threeMonthsAgo)
+            QueryTemplate.EQ("account_id__c", accountId),
+            QueryTemplate.EQ("life_status", "normal"),
+            QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
         ]))
         .limit(500)
@@ -41,19 +28,13 @@
 log.info("[业务] 查到提货单数量: " + (findResult ? findResult.size : 0))
 
-// ----- 4. 汇总提货总额 -----
 BigDecimal totalAmount = BigDecimal.ZERO
-if (findResult && findResult.dataList && findResult.dataList.size() > 0) {
+if (findResult?.dataList) {
     findResult.dataList.each { item ->
-        Map row = item as Map
-        Object val = row["total_amount__c"]
-        if (val != null) {
-            totalAmount = totalAmount.add(new BigDecimal(val.toString()))
-        }
+        Object val = (item as Map)["total_amount__c"]
+        if (val != null) totalAmount = totalAmount.add(new BigDecimal(val.toString()))
     }
 }
 log.info("[业务] 汇总提货总额: " + totalAmount)
 
-// ----- 5. 更新客户的近一个月回款字段 -----
-log.info("[业务] 更新客户 recent_1m_repayment__c: accountId=" + accountId + ", value=" + totalAmount)
 def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update(
     "AccountObj",

```

## 第 2 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-71721754
16:20:27:820 [业务] 入参: deliveryOrderId=69c0b28756646900086dae54, accountId=69ba0e00d4d66f000716061d
16:20:27:828 [业务] 查询条件: accountId=69ba0e00d4d66f000716061d, passing_date__c >= 1766564427828
16:20:27:832 [业务] 查询提货单失败: The number of keys in the map cannot exceed 1
StopWatch 'Proc_Hb4Jz__c': running time = 269 ms
------------------------------------------
```

**代码变更（unified diff）：**
```diff
--- v1
+++ v2
@@ -1,4 +1,2 @@
-错误原因：`QueryTemplate.AND()` 传入的 Map 超过1个 key 时报错 `The number of keys in the map cannot exceed 1`，需改为将多个条件以列表形式传入。
-
 String deliveryOrderId = context.data["_id"] as String
 String accountId = context.data["account_id__c"] as String
@@ -19,5 +17,5 @@
             QueryTemplate.EQ("life_status", "normal"),
             QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
-        ]))
+        ] as List))
         .limit(500)
         .build(),

```

## 第 3 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-51473737
StopWatch 'Proc_Hb4Jz__c': running time = 204 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 35.
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -18,5 +18,4 @@
             QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
         ] as List))
-        .limit(500)
         .build(),
     SelectAttribute.builder().build()

```

## 第 4 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-86814506
StopWatch 'Proc_Hb4Jz__c': running time = 222 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#EQ(java.lang.String, java.lang.String). Please check if the declared type is correct and if the method exists.
 @ line 16, column 13.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#EQ(java.lang.String, java.lang.String). 
```

**代码变更（unified diff）：**
```diff
--- v3
+++ v4
@@ -14,8 +14,8 @@
         .columns(["_id", "total_amount__c"])
         .queryTemplate(QueryTemplate.AND([
-            QueryTemplate.EQ("account_id__c", accountId),
-            QueryTemplate.EQ("life_status", "normal"),
-            QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
-        ] as List))
+            QueryTemplate.EQ("account_id__c", QueryOperator.EQ(accountId)),
+            QueryTemplate.EQ("life_status", QueryOperator.EQ("normal")),
+            QueryTemplate.EQ("passing_date__c", QueryOperator.GTE(threeMonthsAgo))
+        ] as List<QueryTemplate>))
         .build(),
     SelectAttribute.builder().build()

```

## 第 5 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-54734305
StopWatch 'Proc_Hb4Jz__c': running time = 192 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#EQ(java.lang.String, java.lang.String). Please check if the declared type is correct and if the method exists.
 @ line 16, column 13.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#EQ(java.lang.String, java.lang.String). 
```

**代码变更（unified diff）：**
```diff
--- v4
+++ v5
@@ -14,7 +14,7 @@
         .columns(["_id", "total_amount__c"])
         .queryTemplate(QueryTemplate.AND([
-            QueryTemplate.EQ("account_id__c", QueryOperator.EQ(accountId)),
-            QueryTemplate.EQ("life_status", QueryOperator.EQ("normal")),
-            QueryTemplate.EQ("passing_date__c", QueryOperator.GTE(threeMonthsAgo))
+            QueryTemplate.EQ("account_id__c", accountId),
+            QueryTemplate.EQ("life_status", "normal"),
+            QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
         ] as List<QueryTemplate>))
         .build(),

```

## 第 6 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-94385479
StopWatch 'Proc_Hb4Jz__c': running time = 258 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#EQ(java.lang.String, com.fxiaoke.functions.tools.QueryOperator). Please check if the declared type is correct and if the method exists.
 @ line 16, column 13.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#EQ(java.lang.St
```

**代码变更（unified diff）：**
```diff
--- v5
+++ v6
@@ -14,8 +14,8 @@
         .columns(["_id", "total_amount__c"])
         .queryTemplate(QueryTemplate.AND([
-            QueryTemplate.EQ("account_id__c", accountId),
-            QueryTemplate.EQ("life_status", "normal"),
-            QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
-        ] as List<QueryTemplate>))
+            QueryTemplate.EQ("account_id__c", QueryOperator.EQ(accountId)),
+            QueryTemplate.EQ("life_status", QueryOperator.EQ("normal")),
+            QueryTemplate.GTE("passing_date__c", QueryOperator.GTE(threeMonthsAgo))
+        ]))
         .build(),
     SelectAttribute.builder().build()

```

