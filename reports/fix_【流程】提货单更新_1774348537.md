# APL 修复报告：【流程】提货单更新

生成时间：2026-03-24 18:35:37

共修复 6 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-6508658
18:34:05:575 [业务] 入参: billId=69c263ee78b26100071fb667, accountId=69ba0e00d4d66f000716061d
18:34:05:582 [业务] 查询条件: accountId=69ba0e00d4d66f000716061d, 审核通过日期起=1766572445582
18:34:05:585 [业务] 查询提货单失败: The number of keys in the map cannot exceed 1
StopWatch 'Proc_rzhex__c': running time = 256 ms
---------------------------------------------
ms % Task name
----
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -1,15 +1,4 @@
-/**
- * @author 纷享实施人员
- * @codeName 【流程】提货单更新
- * @description 生成一个流程函数
- * 查询提货单上的“门店名称”
- * 查询当前门店名称下的生命状态为正常的提货单，
- * 统计提货单上“审核通过日期”为3个月内的提货总额（元），汇总后更新至客户的“近一个月回款”字段内
- * @createTime 2026-03-24
- * @bindingObjectLabel 提货单
- * @bindingObjectApiName bill_of_lading__c
- * @函数需求编号
- */
-// ----- 1. 取当前提货单数据 -----
+错误原因：`QueryTemplate.AND()` 传入的 Map 超过1个 key 时报错 `The number of keys in the map cannot exceed 1`，需改为链式多条件写法。
+
 String billId = context.data["_id"] as String
 String accountId = context.data["account_id__c"] as String
@@ -19,9 +8,7 @@
 if (!accountId) { log.error("[业务] 终止: 门店名称(account_id__c)为空"); return }
 
-// ----- 2. 计算3个月前时间戳 -----
 Long threeMonthsAgo = System.currentTimeMillis() - (90L * 24 * 60 * 60 * 1000)
 log.info("[业务] 查询条件: accountId=" + accountId + ", 审核通过日期起=" + threeMonthsAgo)
 
-// ----- 3. 查询同门店、生命状态正常、审核通过日期在3个月内的提货单 -----
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "bill_of_lading__c",
@@ -29,7 +16,7 @@
         .columns(["_id", "total_amount__c"])
         .queryTemplate(QueryTemplate.AND([
-            "account_id__c"  : QueryOperator.EQ(accountId),
-            "life_status"    : QueryOperator.EQ("normal"),
-            "passing_date__c": QueryOperator.GTE(threeMonthsAgo)
+            QueryTemplate.EQ("account_id__c", accountId),
+            QueryTemplate.EQ("life_status", "normal"),
+            QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
         ]))
         .build(),
@@ -39,25 +26,13 @@
 if (findErr) { log.error("[业务] 查询提货单失败: " + findMsg); return }
 
-int recordCount = 0
-if (findResult && findResult.dataList) {
-    recordCount = findResult.dataList.size()
-}
-log.info("[业务] 查到提货单数量: " + recordCount)
-
-// ----- 4. 汇总提货总额 -----
 BigDecimal totalAmount = BigDecimal.ZERO
-if (findResult && findResult.dataList) {
+if (findResult?.dataList) {
     findResult.dataList.each { item ->
-        Map row = item as Map
-        Object amtVal = row.get("total_amount__c")
-        if (amtVal != null) {
-            totalAmount = totalAmount.add(new BigDecimal(amtVal.toString()))
-        }
+        Object amtVal = (item as Map).get("total_amount__c")
+        if (amtVal != null) totalAmount = totalAmount.add(new BigDecimal(amtVal.toString()))
     }
 }
-log.info("[业务] 汇总提货总额: " + totalAmount)
+log.info("[业务] 查到提货单数量: " + (findResult?.dataList?.size() ?: 0) + ", 汇总总额: " + totalAmount)
 
-// ----- 5. 更新客户的"近一个月回款"字段 -----
-log.info("[业务] 更新客户 recent_1m_repayment__c: accountId=" + accountId + ", value=" + totalAmount)
 def (Boolean updateErr, Map updateResult, String updateMsg) = Fx.object.update(
     "AccountObj",
@@ -70,4 +45,4 @@
     log.error("[业务] 更新客户失败: " + updateMsg)
 } else {
-    log.info("[业务] 完成: 客户近一个月回款已更新为 " + totalAmount + ", updateResult=" + updateResult)
+    log.info("[业务] 完成: 近一个月回款已更新为 " + totalAmount)
 }
```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-59617614
StopWatch 'Proc_rzhex__c': running time = 203 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected char: '`' @ line 1, column 35.
```

**代码变更（unified diff）：**
```diff
--- v1
+++ v2
@@ -1,4 +1,2 @@
-错误原因：`QueryTemplate.AND()` 传入的 Map 超过1个 key 时报错 `The number of keys in the map cannot exceed 1`，需改为链式多条件写法。
-
 String billId = context.data["_id"] as String
 String accountId = context.data["account_id__c"] as String
@@ -15,9 +13,9 @@
     FQLAttribute.builder()
         .columns(["_id", "total_amount__c"])
-        .queryTemplate(QueryTemplate.AND([
-            QueryTemplate.EQ("account_id__c", accountId),
-            QueryTemplate.EQ("life_status", "normal"),
-            QueryTemplate.GTE("passing_date__c", threeMonthsAgo)
-        ]))
+        .queryTemplate(
+            QueryTemplate.AND(["account_id__c": QueryOperator.EQ(accountId)])
+                .and(QueryTemplate.AND(["life_status": QueryOperator.EQ("normal")]))
+                .and(QueryTemplate.AND(["passing_date__c": QueryOperator.GTE(threeMonthsAgo)]))
+        )
         .build(),
     SelectAttribute.builder().build()

```

## 第 3 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-81976296
StopWatch 'Proc_rzhex__c': running time = 220 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.QueryTemplate#and(com.fxiaoke.functions.model.QueryTemplate). Please check if the declared type is correct and if the method exists.
 @ line 16, column 13.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.model.FQLAttribute$FQLAttributeBuilder#queryTemplate(j
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -9,13 +9,11 @@
 log.info("[业务] 查询条件: accountId=" + accountId + ", 审核通过日期起=" + threeMonthsAgo)
 
+QueryTemplate qt = QueryTemplate.AND(["account_id__c": QueryOperator.EQ(accountId), "life_status": QueryOperator.EQ("normal"), "passing_date__c": QueryOperator.GTE(threeMonthsAgo)])
+
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "bill_of_lading__c",
     FQLAttribute.builder()
         .columns(["_id", "total_amount__c"])
-        .queryTemplate(
-            QueryTemplate.AND(["account_id__c": QueryOperator.EQ(accountId)])
-                .and(QueryTemplate.AND(["life_status": QueryOperator.EQ("normal")]))
-                .and(QueryTemplate.AND(["passing_date__c": QueryOperator.GTE(threeMonthsAgo)]))
-        )
+        .queryTemplate(qt)
         .build(),
     SelectAttribute.builder().build()

```

## 第 4 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-42703229
18:34:59:220 [业务] 入参: billId=null, accountId=null
18:34:59:226 [业务] 终止: 提货单ID为空
StopWatch 'Proc_rzhex__c': running time = 322 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
```

**代码变更（unified diff）：**
```diff
--- v3
+++ v4
@@ -1,4 +1,8 @@
-String billId = context.data["_id"] as String
-String accountId = context.data["account_id__c"] as String
+APL 中触发器/流程的入参通常从 `context.trigger` 或 `context.object` 取，不是 `context.data`。
+
+修复：将取值改为 `context.object` 并保留 `context.data` 作为兜底。
+
+String billId = (context.object?["_id"] ?: context.data?["_id"]) as String
+String accountId = (context.object?["account_id__c"] ?: context.data?["account_id__c"]) as String
 log.info("[业务] 入参: billId=" + billId + ", accountId=" + accountId)
 

```

## 第 5 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-58351149
StopWatch 'Proc_rzhex__c': running time = 64 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected token: 中触发器 @ line 1, column 34.
```

**代码变更（unified diff）：**
```diff
--- before

+++ after

@@ -3,6 +3,6 @@

 修复：将取值改为 `context.object` 并保留 `context.data` 作为兜底。

 

-String billId = (context.object?["_id"] ?: context.data?["_id"]) as String

-String accountId = (context.object?["account_id__c"] ?: context.data?["account_id__c"]) as String

+String billId = (context.object?.get("_id") ?: context.data?.get("_id")) as String

+String accountId = (context.object?.get("account_id__c") ?: context.data?.get("account_id__c")) as String

 log.info("[业务] 入参: billId=" + billId + ", accountId=" + accountId)

 


```

## 第 6 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-77337630
StopWatch 'Proc_rzhex__c': running time = 384 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

unexpected token: 中触发器 @ line 1, column 34.
```

**代码变更（unified diff）：**
```diff
--- v5
+++ v6
@@ -1,8 +1,6 @@
-APL 中触发器/流程的入参通常从 `context.trigger` 或 `context.object` 取，不是 `context.data`。
-
-修复：将取值改为 `context.object` 并保留 `context.data` 作为兜底。
-
-String billId = (context.object?.get("_id") ?: context.data?.get("_id")) as String
-String accountId = (context.object?.get("account_id__c") ?: context.data?.get("account_id__c")) as String
+String billId = context.object?.get("_id") as String
+if (!billId) { billId = context.data?.get("_id") as String }
+String accountId = context.object?.get("account_id__c") as String
+if (!accountId) { accountId = context.data?.get("account_id__c") as String }
 log.info("[业务] 入参: billId=" + billId + ", accountId=" + accountId)
 

```

