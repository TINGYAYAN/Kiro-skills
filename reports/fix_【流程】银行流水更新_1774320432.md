# APL 修复报告：【流程】银行流水更新

生成时间：2026-03-24 10:47:12

共修复 3 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-5180591
StopWatch 'Proc_yYaKH__c': running time = 232 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

SecurityException: ForStatements are not allowed
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -2,5 +2,5 @@
  * @author 纷享实施人员
  * @codeName 【流程】银行流水更新
- * @description 计划任务，查询"银行流水"对象"日期"据当前日期1个月内容全部数据，汇总"打款金额"，更新汇总金额到"客户"对象上的"近一个季度回款"字段上
+ * @description 计划任务,查询"银行流水"对象"日期"据当前日期1个月内容全部数据,汇总"打款金额",更新汇总金额到"客户"对象上的"近一个季度回款"字段上
  * @createTime 2026-03-24
  * @bindingObjectLabel 银行流水
@@ -8,9 +8,7 @@
  * @函数需求编号
  */
-// ----- 1. 计算1个月前的时间戳 -----
 long oneMonthAgo = System.currentTimeMillis() - (30L * 24 * 60 * 60 * 1000)
 log.info("[业务] 入参: 查询日期 >= " + oneMonthAgo)
 
-// ----- 2. 查询银行流水（1个月内） -----
 log.info("[业务] 按日期查询银行流水")
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
@@ -35,9 +33,8 @@
 log.info("[业务] 查到 " + findResult.dataList.size() + " 条银行流水")
 
-// ----- 3. 按客户名称分组汇总打款金额 -----
 Map<String, BigDecimal> customerAmountMap = [:]
-for (Map record : findResult.dataList) {
+findResult.dataList.each { Map record ->
     String customerName = record.store_name__c as String
-    if (!customerName) continue
+    if (!customerName) return
     
     Object amountObj = record.transfer_amount__c
@@ -56,11 +53,7 @@
 }
 
-log.info("[业务] 汇总完成，涉及 " + customerAmountMap.size() + " 个客户")
+log.info("[业务] 汇总完成,涉及 " + customerAmountMap.size() + " 个客户")
 
-// ----- 4. 按客户名称查询并更新近一个季度回款 -----
-for (Map.Entry<String, BigDecimal> entry : customerAmountMap.entrySet()) {
-    String customerName = entry.getKey()
-    BigDecimal totalAmount = entry.getValue()
-    
+customerAmountMap.each { String customerName, BigDecimal totalAmount ->
     log.info("[业务] 处理客户: " + customerName + ", 汇总金额: " + totalAmount)
     
@@ -76,10 +69,10 @@
     if (queryErr) {
         log.error("[业务] 查询客户失败: " + queryMsg + ", 客户名称: " + customerName)
-        continue
+        return
     }
     
     if (!queryResult || !queryResult.dataList || queryResult.dataList.size() == 0) {
         log.info("[业务] 未找到客户: " + customerName)
-        continue
+        return
     }
     

```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-59743672
StopWatch 'Proc_yYaKH__c': running time = 280 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

Expected parameter of type java.lang.Object but got java.util.Map
 @ line 36, column 28.
[Static type checking] - No such property: store_name__c for class: java.lang.Object
 @ line 37, column 27.
[Static type checking] - No such property: transfer_amount__c for class: java.lang.Object
 @ line 40, column 24.
```

**代码变更（unified diff）：**
```diff
--- v1
+++ v2
@@ -34,5 +34,6 @@
 
 Map<String, BigDecimal> customerAmountMap = [:]
-findResult.dataList.each { Map record ->
+findResult.dataList.each { Object item ->
+    Map record = item as Map
     String customerName = record.store_name__c as String
     if (!customerName) return

```

## 第 3 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-59743672
StopWatch 'Proc_yYaKH__c': running time = 280 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

Expected parameter of type java.lang.Object but got java.util.Map
 @ line 36, column 28.
[Static type checking] - No such property: store_name__c for class: java.lang.Object
 @ line 37, column 27.
[Static type checking] - No such property: transfer_amount__c for class: java.lang.Object
 @ line 40, column 24.
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -34,10 +34,9 @@
 
 Map<String, BigDecimal> customerAmountMap = [:]
-findResult.dataList.each { Object item ->
-    Map record = item as Map
-    String customerName = record.store_name__c as String
+findResult.dataList.each { Map item ->
+    String customerName = item.store_name__c as String
     if (!customerName) return
     
-    Object amountObj = record.transfer_amount__c
+    Object amountObj = item.transfer_amount__c
     BigDecimal amount = BigDecimal.ZERO
     if (amountObj != null) {

```

