# APL 修复报告：【计划任务】关联客户

生成时间：2026-03-19 16:19:33

共修复 6 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-35418527
StopWatch 'Proc_dxRb7__c': running time = 504 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method java.util.Calendar#setTime(com.fxiaoke.functions.time.Date). Please check if the declared type is correct and if the method exists.
 @ line 13, column 1.
[Static type checking] - Cannot assign value of type java.util.Date to variable of type com.fxiaoke.functions.time.Date
 @ line 15, column 20.
[Static type che
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -2,5 +2,5 @@
  * @author 纷享实施人员
  * @codeName 【计划任务】关联客户
- * @description 创建计划任务函数。查询银行流水对象日期据当前日期1个月内的全部数据，汇总打款金额，将汇总金额更新到客户对象上的近一个季度回款字段上。银行流水对象APIname：payment_record__c，银行流水的日期字段 API 名：date__c，银行流水的打款金额字段 API 名：transfer_amount__c，银行流水关联客户的字段 API 名：store_name__c，客户的近一个季度回款字段 API 名：quarterly_repayment__c
+ * @description 创建计划任务函数。查询银行流水对象日期据当前日期1个月内的全部数据,汇总打款金额,将汇总金额更新到客户对象上的近一个季度回款字段上。银行流水对象APIname:payment_record__c,银行流水的日期字段 API 名:date__c,银行流水的打款金额字段 API 名:transfer_amount__c,银行流水关联客户的字段 API 名:store_name__c,客户的近一个季度回款字段 API 名:quarterly_repayment__c
  * @createTime 2026-03-19
  * @bindingObjectLabel 银行流水
@@ -8,15 +8,15 @@
  * @函数需求编号
  */
-// ----- 1. 计算日期范围（当前日期前1个月） -----
+// ----- 1. 计算日期范围(当前日期前1个月) -----
 Date now = new Date()
 Calendar cal = Calendar.getInstance()
-cal.setTime(now)
+cal.setTime(now.toJavaDate())
 cal.add(Calendar.MONTH, -1)
-Date oneMonthAgo = cal.getTime()
+Date oneMonthAgo = new Date(cal.getTime())
 
-log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo.format("yyyy-MM-dd"))
+log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo.toString("yyyy-MM-dd"))
 
-// ----- 2. 查询银行流水（1个月内） -----
-log.info("[业务] 查询银行流水对象，日期 >= " + oneMonthAgo.format("yyyy-MM-dd"))
+// ----- 2. 查询银行流水(1个月内) -----
+log.info("[业务] 查询银行流水对象,日期 >= " + oneMonthAgo.toString("yyyy-MM-dd"))
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "payment_record__c",
@@ -42,5 +42,6 @@
 // ----- 3. 按客户分组汇总打款金额 -----
 Map<String, BigDecimal> customerAmountMap = [:]
-findResult.dataList.each { Map flow ->
+findResult.dataList.each { Object flowObj ->
+    Map flow = flowObj as Map
     String customerId = flow["store_name__c"] as String
     Object amountObj = flow["transfer_amount__c"]
@@ -56,5 +57,5 @@
 }
 
-log.info("[业务] 汇总完成，涉及 " + customerAmountMap.size() + " 个客户")
+log.info("[业务] 汇总完成,涉及 " + customerAmountMap.size() + " 个客户")
 
 // ----- 4. 批量更新客户的近一个季度回款字段 -----

```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-95627581
StopWatch 'Proc_dxRb7__c': running time = 286 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.time.Date#toJavaDate(). Please check if the declared type is correct and if the method exists.
 @ line 13, column 13.
[Static type checking] - Cannot find matching method java.util.Calendar#setTime(java.lang.Object). Please check if the declared type is correct and if the method exists.
 @ 
```

**代码变更（unified diff）：**
```diff
--- v1
+++ v2
@@ -8,15 +8,10 @@
  * @函数需求编号
  */
-// ----- 1. 计算日期范围(当前日期前1个月) -----
 Date now = new Date()
-Calendar cal = Calendar.getInstance()
-cal.setTime(now.toJavaDate())
-cal.add(Calendar.MONTH, -1)
-Date oneMonthAgo = new Date(cal.getTime())
+Date oneMonthAgo = now.addMonths(-1)
 
-log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo.toString("yyyy-MM-dd"))
+log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo.format("yyyy-MM-dd"))
 
-// ----- 2. 查询银行流水(1个月内) -----
-log.info("[业务] 查询银行流水对象,日期 >= " + oneMonthAgo.toString("yyyy-MM-dd"))
+log.info("[业务] 查询银行流水对象,日期 >= " + oneMonthAgo.format("yyyy-MM-dd"))
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "payment_record__c",
@@ -40,5 +35,4 @@
 log.info("[业务] 查到 " + findResult.dataList.size() + " 条银行流水记录")
 
-// ----- 3. 按客户分组汇总打款金额 -----
 Map<String, BigDecimal> customerAmountMap = [:]
 findResult.dataList.each { Object flowObj ->
@@ -59,5 +53,4 @@
 log.info("[业务] 汇总完成,涉及 " + customerAmountMap.size() + " 个客户")
 
-// ----- 4. 批量更新客户的近一个季度回款字段 -----
 customerAmountMap.each { String customerId, BigDecimal totalAmount ->
     log.info("[业务] 更新客户 " + customerId + " 的回款金额: " + totalAmount)

```

## 第 3 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-19836951
StopWatch 'Proc_dxRb7__c': running time = 157 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.time.Date#addMonths(java.lang.Integer). Please check if the declared type is correct and if the method exists.
 @ line 11, column 20.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.time.Date#format(java.lang.String). Please check if the declared type is correct a
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -8,10 +8,10 @@
  * @函数需求编号
  */
-Date now = new Date()
-Date oneMonthAgo = now.addMonths(-1)
+String now = Fx.time.now()
+String oneMonthAgo = Fx.time.addMonths(now, -1)
 
-log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo.format("yyyy-MM-dd"))
+log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo)
 
-log.info("[业务] 查询银行流水对象,日期 >= " + oneMonthAgo.format("yyyy-MM-dd"))
+log.info("[业务] 查询银行流水对象,日期 >= " + oneMonthAgo)
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "payment_record__c",

```

## 第 4 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-87402859
StopWatch 'Proc_dxRb7__c': running time = 252 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - No such property: time for class: java.lang.Class 
 @ line 10, column 14.
[Static type checking] - No such property: time for class: java.lang.Class 
 @ line 11, column 22.
```

**代码变更（unified diff）：**
```diff
--- v3
+++ v4
@@ -8,6 +8,6 @@
  * @函数需求编号
  */
-String now = Fx.time.now()
-String oneMonthAgo = Fx.time.addMonths(now, -1)
+String now = Fx.date.now()
+String oneMonthAgo = Fx.date.addMonths(now, -1)
 
 log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo)

```

## 第 5 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-49511690
StopWatch 'Proc_dxRb7__c': running time = 217 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - No such property: date for class: java.lang.Class 
 @ line 10, column 14.
[Static type checking] - No such property: date for class: java.lang.Class 
 @ line 11, column 22.
```

**代码变更（unified diff）：**
```diff
--- v4
+++ v5
@@ -8,6 +8,6 @@
  * @函数需求编号
  */
-String now = Fx.date.now()
-String oneMonthAgo = Fx.date.addMonths(now, -1)
+String now = Fx.date().now()
+String oneMonthAgo = Fx.date().addMonths(now, -1)
 
 log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo)

```

## 第 6 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-61304568
StopWatch 'Proc_dxRb7__c': running time = 177 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

[Static type checking] - Cannot find matching method com.fxiaoke.functions.Fx#date(). Please check if the declared type is correct and if the method exists.
 @ line 10, column 14.
[Static type checking] - Cannot find matching method com.fxiaoke.functions.Fx#date(). Please check if the declared type is correct and if the method exists.
 @ line 11, column 22.
```

**代码变更（unified diff）：**
```diff
--- v5
+++ v6
@@ -8,6 +8,6 @@
  * @函数需求编号
  */
-String now = Fx.date().now()
-String oneMonthAgo = Fx.date().addMonths(now, -1)
+String now = Fx.date.now()
+String oneMonthAgo = Fx.date.addMonths(now, -1)
 
 log.info("[业务] 入参: 查询日期范围 >= " + oneMonthAgo)

```

