# APL 修复报告：【计划任务】银行流水更新

生成时间：2026-03-24 14:18:32

共修复 3 次

## 第 1 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-31896483
StopWatch 'Proc_bytnC__c': running time = 95 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

SecurityException: ForStatements are not allowed
```

**代码变更（unified diff）：**
```diff
--- v0
+++ v1
@@ -1,18 +1,4 @@
-/**
- * @author 纷享实施人员
- * @codeName 【计划任务】银行流水更新
- * @description 计划任务，查询"银行流水"对象"日期"据当前日期1个月内容全部数据，汇总"打款金额"，更汇总金额更新到"客户"对象上的"近一个季度回款"字段上
- * @createTime 2026-03-24
- * @bindingObjectLabel 银行流水
- * @bindingObjectApiName payment_record__c
- * @函数需求编号
- */
-// ----- 1. 计划任务入口 -----
-log.info("[业务] 计划任务开始：汇总近1个月银行流水打款金额，更新客户近一个季度回款")
+long oneMonthAgo = System.currentTimeMillis() - (30L * 24 * 60 * 60 * 1000)
 
-long oneMonthAgo = System.currentTimeMillis() - (30L * 24 * 60 * 60 * 1000)
-log.info("[业务] 查询日期范围: date__c >= " + oneMonthAgo)
-
-// ----- 2. 查询1个月内银行流水 -----
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "payment_record__c",
@@ -20,5 +6,4 @@
         .columns(["_id", "date__c", "transfer_amount__c", "store_name__c"])
         .queryTemplate(QueryTemplate.AND(["date__c": QueryOperator.GTE(oneMonthAgo)]))
-        .limit(500)
         .build(),
     SelectAttribute.builder().build()
@@ -38,28 +23,21 @@
 log.info("[业务] 查到 " + dataList.size() + " 条银行流水记录")
 
-// ----- 3. 按客户分组汇总打款金额 -----
 Map<String, BigDecimal> customerAmountMap = [:]
 
-for (Object item : dataList) {
+dataList.each { Object item ->
     Map flow = item as Map
     String customerId = flow["store_name__c"] as String
     Object amountObj = flow["transfer_amount__c"]
-    if (!customerId || amountObj == null) {
-        continue
+    if (customerId && amountObj != null) {
+        BigDecimal amount = new BigDecimal(amountObj.toString())
+        BigDecimal current = customerAmountMap.get(customerId)
+        if (current == null) { current = BigDecimal.ZERO }
+        customerAmountMap.put(customerId, current.add(amount))
     }
-    BigDecimal amount = new BigDecimal(amountObj.toString())
-    BigDecimal current = customerAmountMap.get(customerId)
-    if (current == null) {
-        current = BigDecimal.ZERO
-    }
-    customerAmountMap.put(customerId, current.add(amount))
 }
 
 log.info("[业务] 汇总完成，涉及 " + customerAmountMap.size() + " 个客户")
 
-// ----- 4. 逐客户更新近一个季度回款 -----
-for (Map.Entry<String, BigDecimal> entry : customerAmountMap.entrySet()) {
-    String cid = entry.getKey()
-    BigDecimal totalAmount = entry.getValue()
+customerAmountMap.each { String cid, BigDecimal totalAmount ->
     log.info("[业务] 更新客户 " + cid + " 近一个季度回款: " + totalAmount)
 
@@ -74,5 +52,5 @@
         log.error("[业务] 更新客户 " + cid + " 失败: " + updateMsg)
     } else {
-        log.info("[业务] 更新客户 " + cid + " 成功，金额: " + totalAmount)
+        log.info("[业务] 更新客户 " + cid + " 成功，金额: " + totalAmount + "，结果id: " + updateResult?["_id"])
     }
 }

```

## 第 2 次修复

**错误信息：**
```
运行脚本 
 扫描函数
运行日志
E-E.bjlrsm.1001-7430557
StopWatch 'Proc_bytnC__c': running time = 214 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

expecting ':', found ')' @ line 54, column 98.
```

**代码变更（unified diff）：**
```diff
--- before

+++ after

@@ -52,5 +52,5 @@

         log.error("[业务] 更新客户 " + cid + " 失败: " + updateMsg)

     } else {

-        log.info("[业务] 更新客户 " + cid + " 成功，金额: " + totalAmount + "，结果id: " + updateResult?["_id"])

+        log.info("[业务] 更新客户 " + cid + " 成功，金额: " + totalAmount + "，结果id: " + updateResult?.get("_id"))

     }

 }


```

## 第 3 次修复

**错误信息：**
```
运行中... 
 扫描函数
运行日志
E-E.bjlrsm.1001-7430557
StopWatch 'Proc_bytnC__c': running time = 214 ms
---------------------------------------------
ms % Task name
---------------------------------------------


返回值:
 APL代码fix error Action
错误提示

expecting ':', found ')' @ line 54, column 98.
```

**代码变更（unified diff）：**
```diff
--- v2
+++ v3
@@ -1,4 +1,2 @@
-long oneMonthAgo = System.currentTimeMillis() - (30L * 24 * 60 * 60 * 1000)
-
 def (Boolean findErr, QueryResult findResult, String findMsg) = Fx.object.find(
     "payment_record__c",
@@ -52,5 +50,6 @@
         log.error("[业务] 更新客户 " + cid + " 失败: " + updateMsg)
     } else {
-        log.info("[业务] 更新客户 " + cid + " 成功，金额: " + totalAmount + "，结果id: " + updateResult?.get("_id"))
+        String updatedId = updateResult?.get("_id") as String
+        log.info("[业务] 更新客户 " + cid + " 成功，金额: " + totalAmount + "，结果id: " + updatedId)
     }
 }

```

